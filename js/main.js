'use strict';
// var map = document.querySelector('.map');

window.setup.disabledState();
window.form.getCoordinates('start');

// Создаёт моки, чтобы можно было сразу вызвать их двумя разными функциями

// Помещает карточку объявления в разметку перед .map__filters-container
// map.insertBefore(window.card.generateCard(window.data.mocks[window.util.getRandomInt(0, window.data.mocks.length)]), document.querySelector('.map__filters-container'));
